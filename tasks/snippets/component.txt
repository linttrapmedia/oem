import { State } from '@core/framework/State';
import { Template } from '@core/framework/Template';
import { Trait } from '@core/framework/Trait';

type __COMPONENT__ViewState = 'INIT' | 'READY';
type __COMPONENT__Props = {
  state: __COMPONENT__ViewState;
};

export const __COMPONENT__ = ({ state = 'INIT' }: __COMPONENT__Props) => {
  // state
  const _state = State.Atom<__COMPONENT__ViewState>(state);

  // template
  const html = Template.Html({
    style: Trait.Style,
  });

  // elements
  const wrapper = html('div', ['style', 'display', 'flex']);

  // html
  return wrapper(_state.get());
};
